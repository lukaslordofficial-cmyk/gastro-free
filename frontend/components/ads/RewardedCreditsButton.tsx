/**
 * Usunięte — kredyty AI tylko z subskrypcji / top-up.
 * Komponent zostaje jako no-op, żeby stare importy nie wywalały bundlera.
 */
import React from 'react';

type Props = { compact?: boolean; testID?: string };

export function RewardedCreditsButton(_props: Props) {
  return null;
}
